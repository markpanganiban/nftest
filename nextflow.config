process.container = 'quay.io/nextflow/bash'
aws {
    client {
        storageEncryption = 'AES256'
        s3Acl = 'BucketOwnerFullControl'
    }
}

manifest {
    homePage = 'http://foo.com'
    description = 'Pipeline does this and that'
    mainScript = 'manifest.nf'
    version = '1.0.0'
}
